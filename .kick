process do |files|
  files.delete_if do |file|
    file != 'app.rb'
  end
  unless files.empty?
    p 'KICKER: App.rb changed, running rake task'
    Process.spawn('rake kick:run_or_restart_server', :out => STDOUT)
    files.delete('app.rb')
  end
end
